# Voting_System
This Voting System using linked lists for managing Candidates (ID, Name, Votes) and Voters (ID, Voted status). Data is persistently stored in text files (candidates.txt, voters.txt, admin_pass.txt). Features include Admin login, adding/removing candidates, generating voters, casting votes, and displaying/saving sorted results.
